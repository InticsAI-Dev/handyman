package in.handyman.raven.lib;

import com.fasterxml.jackson.databind.ObjectMapper;
import in.handyman.raven.lambda.action.ActionExecution;
import in.handyman.raven.lambda.action.IActionExecution;
import in.handyman.raven.lambda.doa.audit.ActionExecutionAudit;
import in.handyman.raven.lib.model.StateInfoToJson;

import java.lang.Exception;
import java.lang.Object;
import java.lang.Override;

import org.slf4j.Logger;
import org.slf4j.Marker;
import org.slf4j.MarkerFactory;

/**
 * Auto Generated By Raven
 */
@ActionExecution(
        actionName = "StateInfoToJson"
)
public class StateInfoToJsonAction implements IActionExecution {
    private final ActionExecutionAudit action;

    private final Logger log;

    private final StateInfoToJson stateInfoToJson;

    private final Marker aMarker;

    private static final ObjectMapper MAPPER = new ObjectMapper();

    public StateInfoToJsonAction(final ActionExecutionAudit action, final Logger log,
                                 final Object stateInfoToJson) {
        this.stateInfoToJson = (StateInfoToJson) stateInfoToJson;
        this.action = action;
        this.log = log;
        this.aMarker = MarkerFactory.getMarker(" StateInfoToJson:" + this.stateInfoToJson.getName());
    }

    @Override
    public void execute() throws Exception {
        String jsonPayload = MAPPER.writeValueAsString(action.getContext());
        action.getContext().put(stateInfoToJson.getTargetVariable(), jsonPayload);
    }

    @Override
    public boolean executeIf() throws Exception {
        return stateInfoToJson.getCondition();
    }
}
