package in.handyman.raven.lib;

import in.handyman.raven.core.encryption.SecurityEngine;
import in.handyman.raven.core.encryption.inticsgrity.InticsIntegrity;
import in.handyman.raven.exception.HandymanException;
import in.handyman.raven.lambda.access.ResourceAccess;
import in.handyman.raven.lambda.action.ActionExecution;
import in.handyman.raven.lambda.action.IActionExecution;
import in.handyman.raven.lambda.doa.audit.ActionExecutionAudit;
import in.handyman.raven.lib.adapters.ocr.*;
import in.handyman.raven.lib.model.OcrTextComparator;
import in.handyman.raven.lib.services.sor.transform.OcrTextComparisonInput;
import in.handyman.raven.util.CommonQueryUtil;
import org.jdbi.v3.core.Jdbi;
import org.jdbi.v3.core.result.ResultIterable;
import org.jdbi.v3.core.statement.Query;
import org.slf4j.Logger;
import org.slf4j.Marker;
import org.slf4j.MarkerFactory;

import java.time.LocalDateTime;
import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

import static in.handyman.raven.core.enums.DatabaseConstants.DB_INSERT_WRITE_BATCH_SIZE;
import static in.handyman.raven.core.enums.EncryptionConstants.ENCRYPT_DEEP_SIFT_OUTPUT;
import static in.handyman.raven.core.enums.EncryptionConstants.ENCRYPT_ITEM_WISE_ENCRYPTION;

/**
 * Auto Generated By Raven - Optimized Version
 */
@ActionExecution(actionName = "OcrTextComparator")
public class OcrTextComparatorAction implements IActionExecution {
    private final ActionExecutionAudit action;
    private final Logger log;
    private final OcrTextComparator ocrTextComparator;
    private final Marker aMarker;
    private final List<OcrTextComparisonInput> OcrTextComparisonInputs = new ArrayList<>();

    private final OcrEncryptionHandler encryptionHandler;
    private final OcrDatabaseHandler databaseHandler;

    public OcrTextComparatorAction(final ActionExecutionAudit action, final Logger log,
            final Object ocrTextComparator) {
        this(action, log, ocrTextComparator, null, null);
    }

    protected OcrTextComparatorAction(final ActionExecutionAudit action, final Logger log,
            final Object ocrTextComparator,
            final OcrEncryptionHandler encryptionHandler,
            final OcrDatabaseHandler databaseHandler) {
        this.ocrTextComparator = (OcrTextComparator) ocrTextComparator;
        this.action = action;
        this.log = log;
        this.aMarker = MarkerFactory.getMarker(
                "OcrTextComparator:" + (ocrTextComparator != null ? this.ocrTextComparator.getName() : "unknown"));
        InticsIntegrity encryption = SecurityEngine.getInticsIntegrityMethod(action, log);

        if (encryptionHandler == null) {
            this.encryptionHandler = new OcrEncryptionHandler(log, aMarker, encryption);
        } else {
            this.encryptionHandler = encryptionHandler;
        }

        if (databaseHandler == null) {
            this.databaseHandler = new OcrDatabaseHandler(log, aMarker, action);
        } else {
            this.databaseHandler = databaseHandler;
        }
    }

    @Override
    public void execute() throws Exception {
        try {
            LocalDateTime executionStartTime = LocalDateTime.now();

            log.info(aMarker, "Starting OCR text comparator execution at: {}", executionStartTime);

            if (ocrTextComparator == null) {
                throw new HandymanException("OcrTextComparator configuration is null");
            }
            final Jdbi jdbi = ResourceAccess.rdbmsJDBIConn(ocrTextComparator.getResourceConn());

            final String pipelineEndToEndEncryptionActivatorStr = action.getContext().get(ENCRYPT_ITEM_WISE_ENCRYPTION);
            final boolean pipelineEndToEndEncryptionActivator = Boolean.parseBoolean(
                    pipelineEndToEndEncryptionActivatorStr != null ? pipelineEndToEndEncryptionActivatorStr : "false");

            final String deepSiftOutputActivatorStr = action.getContext().get(ENCRYPT_DEEP_SIFT_OUTPUT);
            final boolean deepSiftOutputActivator = Boolean
                    .parseBoolean(deepSiftOutputActivatorStr != null ? deepSiftOutputActivatorStr : "false");

            final String batchId = ocrTextComparator.getBatchId() != null ? ocrTextComparator.getBatchId() : "";
            final String outputTableName = ocrTextComparator.getOutputTable() != null
                    ? ocrTextComparator.getOutputTable()
                    : "";
            final int fuzzyMatchThresholdInt = Integer
                    .parseInt(action.getContext().get("ocr.comparison.fuzzy.match.threshold"));
            final double fuzzyMatchThreshold = fuzzyMatchThresholdInt / 100.0;

            jdbi.useTransaction(handle -> {
                final List<String> formattedQuery = CommonQueryUtil.getFormattedQuery(ocrTextComparator.getQuerySet());
                for (int i = 0; i < formattedQuery.size(); i++) {
                    String sqlToExecute = formattedQuery.get(i);
                    log.info(aMarker, "Executing query {} from index {}", sqlToExecute, i);
                    Query query = handle.createQuery(sqlToExecute);
                    ResultIterable<OcrTextComparisonInput> resultIterable = query
                            .mapToBean(OcrTextComparisonInput.class);
                    List<OcrTextComparisonInput> processingExecutorInputs = resultIterable.stream()
                            .collect(Collectors.toList());
                    OcrTextComparisonInputs.addAll(processingExecutorInputs);
                    log.info(aMarker, "Executed query from index {}", i);
                }
            });

            log.info(aMarker, "OCR text comparator action total rows returned from the query {}",
                    OcrTextComparisonInputs.size());

            try {

                encryptionHandler.performDecryption(OcrTextComparisonInputs, pipelineEndToEndEncryptionActivator,
                        deepSiftOutputActivator);

                List<OcrTextComparisonInput> updatedResults = doOcrTextComparison(OcrTextComparisonInputs,
                        fuzzyMatchThreshold);

                encryptionHandler.performEncryption(updatedResults, pipelineEndToEndEncryptionActivator,
                        deepSiftOutputActivator);

                // Batch insert results
                if (!updatedResults.isEmpty()) {
                    databaseHandler.batchInsertResults(jdbi, outputTableName, updatedResults, executionStartTime);
                }

            } catch (Exception e) {
                log.error(aMarker, "OCR text comparison failed for batchId: {}", batchId, e);
                HandymanException handymanException = new HandymanException(e);
                HandymanException.insertException("OCR text comparison failed", handymanException, action);
            }

            log.info(aMarker, "OCR text comparator action has been completed {}", ocrTextComparator.getName());

        } catch (Exception e) {
            action.getContext().put(ocrTextComparator.getName() + ".isSuccessful", "false");
            log.error(aMarker, "Error in execute method for OCR text comparator", e);
            HandymanException handymanException = new HandymanException(e);
            HandymanException.insertException("OCR text comparator failed", handymanException, action);
        }
    }

    private List<OcrTextComparisonInput> doOcrTextComparison(List<OcrTextComparisonInput> inputs,
            double fuzzyMatchThreshold) {
        OcrTextComparisonExecutor executor = new OcrTextComparisonExecutor(log, aMarker, action, 40); // 8 threads, tune
                                                                                                      // as needed
        List<OcrTextComparisonInput> updatedResults = executor.executeComparisons(inputs, fuzzyMatchThreshold);
        log.info(aMarker, "OCR text comparison completed for {} inputs", updatedResults.size());
        return updatedResults;
    }

    @Override
    public boolean executeIf() throws Exception {
        return ocrTextComparator != null && ocrTextComparator.getCondition();
    }
}